--- 
import { Split } from './'
import { flattenClasses } from '../Style'
import structure from "@globalStructure/sidebarright.json"
import style from "@globalStyle/sidebarright.json"

const props = Astro.props 
const classes = flattenClasses(["left","right","hook"],structure,style,props)
--- 
<Split atts={classes}>
    <slot name="hook" slot="hook" />
    <slot name="left" slot="left" />
    <slot name="right" slot="right" />
</Split>
    


            
